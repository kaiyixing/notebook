# Kubernetes 使用 containerd 作为容器运行时（详细步骤）

这是一个非常好的选择！Kubernetes 从 1.24 版本开始已经移除了 Docker shim，**containerd 是当前生产环境的标准选择**。

------

## 📋 环境准备（所有节点执行）

### 前置系统配置

```bash
# 1. 关闭防火墙
sudo systemctl stop firewalld
sudo systemctl disable firewalld

# 2. 关闭 SELinux
sudo setenforce 0
sudo sed -i 's/SELINUX=enforcing/SELINUX=disabled/g' /etc/selinux/config

# 3. 关闭 Swap
sudo swapoff -a
sudo sed -i '/ swap / s/^\(.*\)$/#\1/g' /etc/fstab




!!!!!!!!!!!!!/etc/resolv.conf不能动，只能添加，在nat模式下

# Generated by NetworkManager
search localdomain
nameserver 192.168.233.2
~！！！！！！！！！！！！！！！
nat
#!/bin/bash
# config-master.sh

echo "===== 配置 Master 节点网络 ====="

# 1. 配置静态IP
echo "[1/4] 配置静态IP..."
nmcli connection modify ens160 \
  ipv4.method manual \
  ipv4.addresses 192.168.233.10/24 \
  ipv4.gateway 192.168.233.2 \
  ipv4.dns "8.8.8.8,114.114.114.114" \
  ipv4.dns-search ""

# 2. 重启网络
echo "[2/4] 重启网络连接..."
nmcli connection down ens160
nmcli connection up ens160

# 3. 配置hosts
echo "[3/4] 配置 hosts 文件..."
cat >> /etc/hosts << EOF
192.168.233.10  k8s-master
192.168.233.11  k8s-node1
192.168.233.12  k8s-node2
EOF

# 4. 验证
echo "[4/4] 验证配置..."
ip addr show ens160 | grep inet
ping -c 2 8.8.8.8 > /dev/null && echo "✓ 外网连通" || echo "✗ 外网不通"
ping -c 2 192.168.233.2 > /dev/null && echo "✓ 网关连通" || echo "✗ 网关不通"

echo "===== 网络配置完成 ====="






# 5. 配置内核参数
sudo cat > /etc/sysctl.d/k8s.conf << EOF
net.bridge.bridge-nf-call-ip6tables = 1
net.bridge.bridge-nf-call-iptables = 1
net.ipv4.ip_forward = 1
EOF
sudo sysctl --system

# 6. 加载内核模块
sudo modprobe br_netfilter
sudo modprobe overlay
sudo modprobe ip_vs
sudo modprobe ip_vs_rr
sudo modprobe ip_vs_wrr
sudo modprobe ip_vs_sh
sudo modprobe nf_conntrack

sudo cat > /etc/modules-load.d/k8s.conf << EOF
br_netfilter
overlay
ip_vs
ip_vs_rr
ip_vs_wrr
ip_vs_sh
nf_conntrack
EOF


# 7. 验证模块
lsmod | grep -E "br_netfilter|overlay|ip_vs"
```

------

## 🔴 步骤 1：安装 containerd（所有节点）

### 方法 A：从官方二进制安装（推荐）

```bash
# 1.1 下载 containerd（以 v1.7.14 为例）
wget https://github.com/containerd/containerd/releases/download/v1.7.14/containerd-1.7.14-linux-amd64.tar.gz

# 1.2 解压到系统目录
sudo tar xvf containerd-1.7.14-linux-amd64.tar.gz -C /usr/local/

# 1.3 验证安装
/usr/local/bin/containerd --version
# 预期：containerd github.com/containerd/containerd v1.7.14 ...

# 1.4 创建 systemd 服务文件
sudo cat > /etc/systemd/system/containerd.service << EOF
[Unit]
Description=containerd container runtime
Documentation=https://containerd.io
After=network.target local-fs.target

[Service]
ExecStartPre=-/sbin/modprobe overlay
ExecStart=/usr/local/bin/containerd

Type=notify
Delegate=yes
KillMode=process
Restart=always
RestartSec=5
LimitNPROC=infinity
LimitCORE=infinity
LimitNOFILE=infinity
TasksMax=infinity
OOMScoreAdjust=-999

[Install]
WantedBy=multi-user.target
EOF

# 1.5 生成默认配置文件
sudo mkdir -p /etc/containerd
sudo /usr/local/bin/containerd config default | sudo tee /etc/containerd/config.toml

# 1.6 修改配置（关键！启用 systemd cgroup 驱动）
sudo sed -i 's/SystemdCgroup = false/SystemdCgroup = true/g' /etc/containerd/config.toml

# 1.7 配置镜像加速器（国内用户必须）
sudo cat >> /etc/containerd/config.toml << EOF

[plugins."io.containerd.grpc.v1.cri".registry.mirrors]
  [plugins."io.containerd.grpc.v1.cri".registry.mirrors."docker.io"]
    endpoint = ["https://registry.aliyuncs.com", "https://docker.m.daocloud.io"]
  [plugins."io.containerd.grpc.v1.cri".registry.mirrors."k8s.gcr.io"]
    endpoint = ["https://registry.aliyuncs.com/google_containers"]
  [plugins."io.containerd.grpc.v1.cri".registry.mirrors."gcr.io"]
    endpoint = ["https://registry.aliyuncs.com/google_containers"]
  [plugins."io.containerd.grpc.v1.cri".registry.mirrors."quay.io"]
    endpoint = ["https://quay.mirrors.ustc.edu.cn"]
EOF

# 1.8 验证配置文件
sudo cat /etc/containerd/config.toml | grep -A 5 "SystemdCgroup"
sudo cat /etc/containerd/config.toml | grep -A 5 "mirrors"

# 1.9 启动 containerd
sudo systemctl daemon-reload
sudo systemctl enable containerd
sudo systemctl start containerd
sudo systemctl status containerd
```

### 方法 B：从 YUM 源安装（CentOS/RHEL）（首选，简单）

```bash
# 添加 Docker 源（containerd 包含在其中）
sudo yum install -y yum-utils
sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo

# 安装 containerd.io
sudo yum install -y containerd.io

# 生成配置
sudo mkdir -p /etc/containerd
sudo containerd config default | sudo tee /etc/containerd/config.toml

# 修改 cgroup 驱动
sudo sed -i 's/SystemdCgroup = false/SystemdCgroup = true/g' /etc/containerd/config.toml

# 使用 sed 修改 containerd 配置文件中的 sandbox 镜像地址
sudo sed -i "s|sandbox = 'registry.k8s.io/pause:3.10.1'|sandbox = 'registry.aliyuncs.com/google_containers/pause:3.10.1'|" /etc/containerd/config.toml
# 启动服务
sudo systemctl enable containerd
sudo systemctl start containerd
```

------

## 🔴 步骤 2：安装 CRI 插件 crictl（所有节点）建议先安装opencode（AI工具，帮助解决问题）

`crictl` 是 Kubernetes 推荐的容器运行时调试工具。

```bash
# 2.1 下载 crictl（版本与 K8s 匹配）
wget https://github.com/kubernetes-sigs/cri-tools/releases/download/v1.28.0/crictl-v1.28.0-linux-amd64.tar.gz

# 2.2 解压到系统路径
sudo tar zxvf crictl-v1.28.0-linux-amd64.tar.gz -C /usr/local/bin   或者直接yum下载也行(推荐)


# 2.3 配置 crictl（指向 containerd）
sudo cat > /etc/crictl.yaml << EOF
runtime-endpoint: unix:///run/containerd/containerd.sock
image-endpoint: unix:///run/containerd/containerd.sock
timeout: 10
debug: false
EOF

# 2.4 验证安装
crictl --version
crictl info
# 预期：显示 containerd 配置信息

# 2.5 常用 crictl 命令（替代 docker 命令）
crictl ps -a          # 查看容器（类似 docker ps -a）
crictl images         # 查看镜像（类似 docker images）
crictl pods           # 查看 Pod 沙箱
crictl logs <ID>      # 查看容器日志
crictl exec -it <ID>  # 进入容器
crictl stats          # 查看资源使用
```

------

## 🔴 步骤 3：安装 Kubernetes 组件（所有节点）

```bash
#!/bin/bash
# 保存为 install-k8s.sh

K8S_VERSION="1.28.15"

echo "===== 安装 Kubernetes v${K8S_VERSION} ====="

# 1. 配置 Kubernetes YUM 源
echo "[1/4] 配置 Kubernetes YUM 源..."
sudo cat > /etc/yum.repos.d/kubernetes.repo << EOF
[kubernetes]
name=Kubernetes
baseurl=https://pkgs.k8s.io/core:/stable:/v1.28/rpm/
enabled=1
gpgcheck=1
gpgkey=https://pkgs.k8s.io/core:/stable:/v1.28/rpm/repodata/repomd.xml.key
EOF

# 2. 安装指定版本
echo "[2/4] 安装 Kubernetes 组件..."
sudo yum install -y --disablerepo=docker-ce-stable kubelet kubeadm kubectl --setopt=gpgcheck=0

# 3. 锁定版本（防止自动升级）
sudo yum --disablerepo=docker-ce-stable install -y yum-plugin-versionlock
sudo yum versionlock kubelet kubeadm kubectl

# 4. 配置 kubelet 使用 containerd
echo "[3/4] 配置 kubelet..."
sudo mkdir -p /etc/systemd/system/kubelet.service.d
sudo cat > /etc/systemd/system/kubelet.service.d/10-containerd.conf << EOF
[Service]
Environment="KUBELET_EXTRA_ARGS=--container-runtime-endpoint=unix:///run/containerd/containerd.sock"
EOF

# 5. 设置开机自启（先不启动）
echo "[4/4] 设置开机自启..."
sudo systemctl enable kubelet
sudo systemctl daemon-reload

# 6. 验证安装
kubeadm version
kubelet --version
kubectl version --client

echo "===== Kubernetes 组件安装完成 ====="
```

------

## 🔴 步骤 4：初始化 Master 节点

```bash
# 仅在 k8s-master 执行

# 4.1 查看需要的镜像
sudo kubeadm config images list

# 4.2 预拉取镜像（使用阿里云镜像仓库）
sudo kubeadm config images pull \
  --image-repository registry.aliyuncs.com/google_containers \
  --cri-socket unix:///run/containerd/containerd.sock 不做也行

# 4.3 初始化集群
sudo kubeadm init \
  --pod-network-cidr=10.244.0.0/16 \
  --apiserver-advertise-address=192.168.233.10 \IP一定要注意
  --kubernetes-version=v1.28.15 \
  --image-repository registry.aliyuncs.com/google_containers \
  --cri-socket unix:///run/containerd/containerd.sock

# 4.4 配置 kubectl
mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config

# 4.5 验证集群
kubectl cluster-info
kubectl get nodes
# 预期：master 显示 NotReady（等待网络插件）
```

------

## 🔴 步骤 5：安装网络插件 CNI

```bash
# 方案 1：Flannel（简单）
kubectl apply -f https://raw.githubusercontent.com/flannel-io/flannel/master/Documentation/kube-flannel.yml

# 方案 2：Calico（推荐，功能更强）
kubectl create -f https://raw.githubusercontent.com/projectcalico/calico/v3.26.1/manifests/tigera-operator.yaml
kubectl create -f https://raw.githubusercontent.com/projectcalico/calico/v3.26.1/manifests/custom-resources.yaml

# 验证 Pod 运行
kubectl get pods -n kube-system
kubectl get pods -n calico-system
```

------

## 🔴 步骤 6：加入 Worker 节点

```bash
# 在 k8s-node1 和 k8s-node2 执行

# 使用 Master 初始化时生成的 join 命令
sudo kubeadm join 192.168.1.10:6443 \
  --token abcdef.0123456789abcdef \
  --discovery-token-ca-cert-hash sha256:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx \
  --cri-socket unix:///run/containerd/containerd.sock

# 如果忘记了 join 命令，在 Master 上重新生成：
kubeadm token create --print-join-command
```

------

## 🔴 步骤 7：验证集群状态

```bash
# 在 Master 节点执行

# 7.1 查看所有节点
kubectl get nodes
# 预期：所有节点显示 Ready

# 7.2 查看系统 Pod
kubectl get pods -n kube-system

# 7.3 使用 crictl 查看容器
sudo crictl ps -a
sudo crictl pods

# 7.4 查看 containerd 镜像
sudo crictl images

# 7.5 部署测试应用
kubectl create deployment nginx --image=nginx
kubectl expose deployment nginx --port=80 --type=NodePort
kubectl get svc
kubectl get pods -o wide

# 7.6 验证 containerd 中的容器
sudo crictl ps | grep nginx
```

------

## 🔴 containerd 常用命令速查表

| 功能         | Docker 命令               | containerd/crictl 命令    |
| ------------ | ------------------------- | ------------------------- |
| 查看容器     | `docker ps`               | `crictl ps`               |
| 查看所有容器 | `docker ps -a`            | `crictl ps -a`            |
| 查看镜像     | `docker images`           | `crictl images`           |
| 拉取镜像     | `docker pull nginx`       | `crictl pull nginx`       |
| 查看日志     | `docker logs <id>`        | `crictl logs <id>`        |
| 进入容器     | `docker exec -it <id> sh` | `crictl exec -it <id> sh` |
| 查看资源     | `docker stats`            | `crictl stats`            |
| 删除容器     | `docker rm <id>`          | `crictl rm <id>`          |
| 删除镜像     | `docker rmi <id>`         | `crictl rmi <id>`         |
| 查看 Pod     | 无                        | `crictl pods`             |
| 查看配置     | `docker info`             | `crictl info`             |

------

## 🔴 故障排查指南

### 问题 1：kubelet 无法连接 containerd

```bash
# 检查 containerd 状态
sudo systemctl status containerd

# 检查 socket 文件
ls -la /run/containerd/containerd.sock

# 查看 kubelet 日志
sudo journalctl -u kubelet -n 50 --no-pager

# 常见错误：connection error
# 解决方案：确保 kubelet 配置了正确的 --container-runtime-endpoint
```

### 问题 2：镜像拉取失败

```bash
# 检查 containerd 配置
sudo cat /etc/containerd/config.toml | grep -A 10 "mirrors"

# 手动拉取镜像测试
sudo crictl pull nginx:latest

# 重启 containerd
sudo systemctl restart containerd

# 清理 containerd 缓存
sudo crictl rmi --prune
```

### 问题 3：cgroup 驱动不匹配

```bash
# 检查 containerd 配置
sudo cat /etc/containerd/config.toml | grep SystemdCgroup
# 必须是：SystemdCgroup = true

# 检查 kubelet 配置
sudo cat /etc/systemd/system/kubelet.service.d/10-containerd.conf

# 重启服务
sudo systemctl daemon-reload
sudo systemctl restart containerd
sudo systemctl restart kubelet
```

### 问题 4：Pod 一直 ContainerCreating

```bash
# 查看 Pod 详情
kubectl describe pod <pod-name>

# 查看 CNI 插件状态
kubectl get pods -n kube-system

# 查看 containerd 日志
sudo journalctl -u containerd -n 50

# 检查网络插件配置
kubectl get configmap -n kube-system
```

------

## 🔴 自动化部署脚本（containerd 版本）（未订正，以上面为准）

```bash
#!/bin/bash
# deploy-k8s-containerd.sh

set -e

MASTER_IP="192.168.1.10"
POD_NETWORK_CIDR="10.244.0.0/16"
K8S_VERSION="1.28.0"
CONTAINERD_VERSION="1.7.14"

log_info() { echo -e "\033[0;32m[INFO]\033[0m $1"; }
log_error() { echo -e "\033[0;31m[ERROR]\033[0m $1"; }

# 系统初始化
init_system() {
    log_info "系统初始化..."
    systemctl stop firewalld || true
    setenforce 0 || true
    swapoff -a
    sed -i '/ swap / s/^\(.*\)$/#\1/g' /etc/fstab
    
    cat > /etc/sysctl.d/k8s.conf << EOF
net.bridge.bridge-nf-call-ip6tables = 1
net.bridge.bridge-nf-call-iptables = 1
net.ipv4.ip_forward = 1
EOF
    sysctl --system
    modprobe br_netfilter
    modprobe overlay
}

# 安装 containerd
install_containerd() {
    log_info "安装 containerd..."
    
    wget -q https://github.com/containerd/containerd/releases/download/v${CONTAINERD_VERSION}/containerd-${CONTAINERD_VERSION}-linux-amd64.tar.gz
    tar xvf containerd-${CONTAINERD_VERSION}-linux-amd64.tar.gz -C /usr/local/
    
    cat > /etc/systemd/system/containerd.service << EOF
[Unit]
Description=containerd container runtime
After=network.target local-fs.target

[Service]
ExecStartPre=-/sbin/modprobe overlay
ExecStart=/usr/local/bin/containerd
Type=notify
Delegate=yes
KillMode=process
Restart=always
RestartSec=5
OOMScoreAdjust=-999

[Install]
WantedBy=multi-user.target
EOF
    
    mkdir -p /etc/containerd
    /usr/local/bin/containerd config default | tee /etc/containerd/config.toml
    sed -i 's/SystemdCgroup = false/SystemdCgroup = true/g' /etc/containerd/config.toml
    
    cat >> /etc/containerd/config.toml << EOF

[plugins."io.containerd.grpc.v1.cri".registry.mirrors]
  [plugins."io.containerd.grpc.v1.cri".registry.mirrors."docker.io"]
    endpoint = ["https://registry.aliyuncs.com"]
  [plugins."io.containerd.grpc.v1.cri".registry.mirrors."k8s.gcr.io"]
    endpoint = ["https://registry.aliyuncs.com/google_containers"]
EOF
    
    systemctl daemon-reload
    systemctl enable containerd
    systemctl start containerd
}

# 安装 crictl
install_crictl() {
    log_info "安装 crictl..."
    wget -q https://github.com/kubernetes-sigs/cri-tools/releases/download/v${K8S_VERSION}/crictl-v${K8S_VERSION}-linux-amd64.tar.gz
    tar zxvf crictl-v${K8S_VERSION}-linux-amd64.tar.gz -C /usr/local/bin
    
    cat > /etc/crictl.yaml << EOF
runtime-endpoint: unix:///run/containerd/containerd.sock
image-endpoint: unix:///run/containerd/containerd.sock
timeout: 10
debug: false
EOF
}

# 安装 K8s 组件
install_k8s() {
    log_info "安装 Kubernetes 组件..."
    
    cat > /etc/yum.repos.d/kubernetes.repo << EOF
[kubernetes]
name=Kubernetes
baseurl=https://pkgs.k8s.io/core:/stable:/v1.28/rpm/
enabled=1
gpgcheck=1
gpgkey=https://pkgs.k8s.io/core:/stable:/v1.28/rpm/repodata/repomd.xml.key
EOF
    
    yum install -y kubelet-${K8S_VERSION} kubeadm-${K8S_VERSION} kubectl-${K8S_VERSION}
    systemctl enable kubelet
    
    mkdir -p /etc/systemd/system/kubelet.service.d
    cat > /etc/systemd/system/kubelet.service.d/10-containerd.conf << EOF
[Service]
Environment="KUBELET_EXTRA_ARGS=--container-runtime-endpoint=unix:///run/containerd/containerd.sock"
EOF
}

# 初始化 Master
init_master() {
    log_info "初始化 Master..."
    
    kubeadm config images pull \
        --image-repository registry.aliyuncs.com/google_containers \
        --cri-socket unix:///run/containerd/containerd.sock
    
    kubeadm init \
        --pod-network-cidr=$POD_NETWORK_CIDR \
        --apiserver-advertise-address=$MASTER_IP \
        --image-repository registry.aliyuncs.com/google_containers \
        --cri-socket unix:///run/containerd/containerd.sock
    
    mkdir -p $HOME/.kube
    cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
    chown $(id -u):$(id -g) $HOME/.kube/config
    
    kubectl apply -f https://raw.githubusercontent.com/flannel-io/flannel/master/Documentation/kube-flannel.yml
    
    kubeadm token create --print-join-command > /tmp/k8s-join-command.sh
    
    log_info "Master 初始化完成"
}

# 加入 Worker
join_worker() {
    log_info "加入 Worker..."
    if [ -f /tmp/k8s-join-command.sh ]; then
        bash /tmp/k8s-join-command.sh
    else
        log_error "未找到 join 命令"
        exit 1
    fi
}

# 主函数
main() {
    case "$1" in
        master)
            init_system
            install_containerd
            install_crictl
            install_k8s
            init_master
            ;;
        worker)
            init_system
            install_containerd
            install_crictl
            install_k8s
            join_worker
            ;;
        *)
            echo "用法: $0 {master|worker}"
            exit 1
            ;;
    esac
}

main "$@"
```

**使用方法：**

```bash
chmod +x deploy-k8s-containerd.sh

# Master 节点
sudo ./deploy-k8s-containerd.sh master

# Worker 节点
sudo ./deploy-k8s-containerd.sh worker

# 验证
kubectl get nodes
crictl ps
```

------

## ✅ 实验完成检查清单

| 检查项          | 命令                              | 预期结果             |
| --------------- | --------------------------------- | -------------------- |
| containerd 运行 | `systemctl status containerd`     | Active: running      |
| crictl 配置     | `crictl info`                     | 显示 containerd 配置 |
| K8s 节点状态    | `kubectl get nodes`               | 所有节点 Ready       |
| 系统 Pod 运行   | `kubectl get pods -n kube-system` | 所有 Pod Running     |
| containerd 容器 | `crictl ps`                       | 显示 K8s 容器        |
| 测试应用        | `kubectl run test --image=nginx`  | Pod 正常运行         |

------

## 💡 面试话术模板

```
【面试官】：你用的什么容器运行时？

【回答】：
我使用的是 containerd，因为 Kubernetes 1.24+ 已经移除了 Docker shim，
containerd 是 CNCF 毕业项目，更轻量、更稳定，也是生产环境的主流选择。

我手动安装配置过 containerd，关键配置包括：
1. 设置 SystemdCgroup = true，与 kubelet 的 cgroup 驱动保持一致
2. 配置镜像加速器，解决国内拉取镜像慢的问题
3. 配置 crictl 工具，用于容器调试和排查

日常排查我用 crictl ps 查看容器，crictl logs 看日志，
crictl exec 进入容器。遇到过 cgroup 不匹配导致 Pod 无法启动，
通过修改 containerd config.toml 解决。

我还写过自动化脚本，用 Ansible 批量部署 containerd + K8s 集群。
```

------

使用 containerd 后，你的技术栈会更贴近当前生产环境，面试时也会更有竞争力！