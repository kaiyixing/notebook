



---

## 一、计算机网络（绝对高频）

| 知识点                           | 频率 | 常见考察形式 / 细节                                          |
| :------------------------------- | :--- | :----------------------------------------------------------- |
| **OSI七层模型 / TCP/IP四层模型** | ⭐⭐⭐  | 各层名称、功能、对应协议（如HTTP在哪层，TCP在哪层），数据封装过程。 |
| **TCP与UDP的区别**               | ⭐⭐⭐  | 面向连接 vs 无连接、可靠 vs 不可靠、首部开销、适用场景（TCP：文件传输，UDP：视频/语音）。 |
| **TCP三次握手与四次挥手**        | ⭐⭐⭐  | 状态变化（SYN_SENT, ESTABLISHED, TIME_WAIT）、序列号作用、为什么是三次/四次、TIME_WAIT过多如何解决。 |
| **HTTP与HTTPS**                  | ⭐⭐⭐  | 默认端口（80 vs 443）、HTTPS加密过程（证书、对称/非对称加密）、HTTP状态码（2xx, 3xx, 4xx, 5xx具体含义如404,502）。 |
| **从输入URL到页面展示全过程**    | ⭐⭐⭐  | DNS解析 -> TCP连接 -> 发送HTTP请求 -> 服务器处理 -> 返回响应 -> 浏览器渲染。 |
| **GET与POST区别**                | ⭐⭐⭐  | 幂等性、数据位置（URL vs Body）、长度限制、安全性。          |
| **DNS工作原理**                  | ⭐⭐   | 递归查询、迭代查询、域名层级、DNS污染/劫持。                 |
| **ARP协议**                      | ⭐⭐   | 地址解析协议作用（IP->MAC）、ARP广播、ARP欺骗、免费ARP。     |
| **ping与traceroute原理**         | ⭐⭐   | 基于ICMP协议，TTL的作用。                                    |
| **Cookie与Session**              | ⭐⭐   | 状态管理、存储位置（客户端 vs 服务器端）、区别与联系。       |
| **WebSocket**                    | ⭐    | 与HTTP的区别、全双工通信、适用场景（实时推送）。             |



---

## 二、Linux 操作系统（绝对高频）

| 知识点                       | 频率 | 常见考察形式 / 细节                                          |
| :--------------------------- | :--- | :----------------------------------------------------------- |
| **常用命令**                 | ⭐⭐⭐  | 文件操作（`ls, cd, cp, mv, rm, find`）、权限（`chmod, chown`）、查看进程（`ps, top, htop`）、网络（`netstat, ss, curl, telnet`）、磁盘（`df, du`）、文本处理（`grep, awk, sed, sort, uniq`）。 |
| **Linux内核与性能调优**      | ⭐⭐   | `/proc`文件系统、内核参数调整（`sysctl`）、文件描述符限制（`ulimit`）、I/O调度器、TCP参数优化。 |
| **进程与线程**               | ⭐⭐   | 区别、上下文切换、僵尸进程与孤儿进程、进程间通信方式（管道、消息队列、共享内存、信号量、Socket）。 |
| **内存管理**                 | ⭐⭐   | 虚拟内存、物理内存、Swap、内存泄漏排查、`free`命令中buffer与cache的区别。 |
| **文件系统**                 | ⭐⭐   | inode、硬链接与软链接、文件权限（rwx, 777含义）、磁盘分区与挂载。 |
| **系统启动流程**             | ⭐    | BIOS/UEFI -> Bootloader -> Kernel -> init（systemd）-> 用户态。 |
| **平均负载（Load Average）** | ⭐    | 含义（1/5/15分钟）、与CPU使用率的区别、如何排查负载过高。    |

---

## 三、容器化与 Kubernetes（K8s）（中高频）

| 知识点              | 频率 | 常见考察形式 / 细节                                          |
| :------------------ | :--- | :----------------------------------------------------------- |
| **Docker基础**      | ⭐⭐⭐  | 镜像与容器的区别、Dockerfile常用指令（`FROM, RUN, CMD, ENTRYPOINT, ADD/COPY`）、多阶段构建、容器网络模式（bridge, host, none）。 |
| **Docker底层技术**  | ⭐⭐   | Namespace（隔离）与 Cgroups（资源限制）、联合文件系统（UnionFS）。 |
| **K8s核心组件**     | ⭐⭐⭐  | Master组件（API Server, Scheduler, Controller Manager, etcd）、Node组件（kubelet, kube-proxy）。 |
| **K8s核心资源对象** | ⭐⭐⭐  | Pod、Deployment、StatefulSet、Service（ClusterIP, NodePort, LoadBalancer）、ConfigMap/Secret、Namespace。 |
| **K8s调度与编排**   | ⭐⭐⭐  | 滚动更新、回滚、探针（liveness, readiness）、服务发现、负载均衡。 |
| **K8s网络**         | ⭐⭐   | CNI、Flannel/Calico原理、Service的iptables/ipvs实现。        |
| **K8s排障**         | ⭐⭐   | Pod启动失败、Image拉取失败、节点NotReady等常见问题排查思路。 |

---

## 四、数据库与中间件（中频）

| 知识点       | 频率 | 常见考察形式 / 细节                                          |
| :----------- | :--- | :----------------------------------------------------------- |
| **MySQL**    | ⭐⭐⭐  | 事务（ACID）、隔离级别（读未提交、读已提交、可重复读、串行化）、索引（B+树）、SQL优化（慢查询排查、explain）、主从复制、引擎区别（InnoDB vs MyISAM）。 |
| **Redis**    | ⭐⭐⭐  | 数据类型（String, Hash, List, Set, Sorted Set）、持久化（RDB, AOF）、缓存问题（穿透、击穿、雪崩）、分布式锁、淘汰策略。 |
| **消息队列** | ⭐⭐   | Kafka/RabbitMQ作用、应用场景（解耦、异步、削峰）、如何保证消息不丢失。 |
| **NoSQL**    | ⭐    | MongoDB等，与关系型数据库的区别。                            |

---

## 五、编程与脚本（中频）

| 知识点             | 频率 | 常见考察形式 / 细节                                          |
| :----------------- | :--- | :----------------------------------------------------------- |
| **Shell脚本**      | ⭐⭐⭐  | 变量、循环、条件判断、常见命令组合（如查找大文件并排序）、正则表达式。 |
| **Python**         | ⭐⭐⭐  | 基础语法、数据结构（列表推导式、生成器）、装饰器、GIL、多线程/多进程。 |
| **Go语言**         | ⭐⭐   | 协程（goroutine）、通道（channel）、GMP模型、与其他语言对比（如Python）。 |
| **数据结构与算法** | ⭐⭐   | 常见手撕题：链表反转、排序（快排、归并）、二叉树遍历、字符串处理、LRU。 |

---

## 六、自动化运维与监控（中频）

| 知识点       | 频率 | 常见考察形式 / 细节                                          |
| :----------- | :--- | :----------------------------------------------------------- |
| **Ansible**  | ⭐⭐   | 选型原因（无agent、基于SSH、易用性）、核心概念（Playbook, Inventory, Module）。 |
| **监控系统** | ⭐⭐   | Prometheus + Grafana架构、监控指标（CPU, 内存, 磁盘IO, 网络）、告警配置。 |
| **CI/CD**    | ⭐⭐   | Jenkins、GitLab CI、流水线设计、蓝绿部署、金丝雀发布。       |
| **日志管理** | ⭐    | ELK/EFK栈（Elasticsearch, Filebeat, Kibana）。               |

---

## 七、岗位认知与软技能（高频）

| 知识点                   | 频率 | 常见考察形式 / 细节                                          |
| :----------------------- | :--- | :----------------------------------------------------------- |
| **为什么选择运维/SRE？** | ⭐⭐⭐  | 对稳定性的追求、对自动化的兴趣、喜欢排查复杂问题、对云原生技术的热情。 |
| **职业规划（3-5年）**    | ⭐⭐⭐  | 成为某个领域的专家（如容器化、数据库运维）、向更高级的SRE或架构师发展、提升自动化与开发能力。 |
| **对重复性工作的看法**   | ⭐⭐   | 通过自动化工具（脚本、平台）减少重复劳动，关注更有价值的工作。 |
| **如何处理压力/加班**    | ⭐⭐   | 表达抗压能力、优先级排序、事后复盘优化。                     |
| **故障排查思路**         | ⭐⭐⭐  | 现象 -> 影响面 -> 日志/监控 -> 定位原因 -> 恢复/回滚 -> 复盘。 |
| **与导师/同事意见不合**  | ⭐⭐   | 先理解对方观点，数据说话，沟通协调，最终服从团队决定。       |

---

## 八、硬件与数据中心网络（低频，但部分岗位高频）

| 知识点               | 频率 | 常见考察形式 / 细节                                          |
| :------------------- | :--- | :----------------------------------------------------------- |
| **VLAN与Trunk**      | ⭐⭐   | 作用（二层隔离）、Access与Trunk端口区别、VLAN间路由。        |
| **STP生成树协议**    | ⭐⭐   | 作用（防环路）、端口状态（Blocking, Listening, Learning, Forwarding）、BID选举。 |
| **BGP/OSPF动态路由** | ⭐⭐   | 协议类型（IGP vs EGP）、OSPF区域、BGP属性（AS-PATH, MED）、防环机制。 |
| **交换机与路由器**   | ⭐⭐   | 核心原理（MAC地址表 vs 路由表）、区别。                      |
| **VRRP/堆叠**        | ⭐    | 网关冗余、设备堆叠的作用。                                   |
| **400G/IB/RDMA**     | ⭐    | 高性能网络相关，多见于特定岗位（如智算中心）。               |

---

## 总结与建议

1.  **计算机网络 + Linux + 容器** 是**三大基石**，几乎必问，且问得很细。
2.  **数据库和Python** 是加分项，特别是MySQL和Redis。
3.  **软技能** 是敲门砖，需要提前准备好话术。
4.  **手撕代码** 逐渐成为标配，重点准备**字符串、链表、排序、简单动态规划**。
5.  **项目经验** 要能讲出**难点、解决方案、优化效果**，而不仅仅是罗列技术栈。

祝你面试顺利！如果有具体某个知识点想深入探讨，可以随时问我。